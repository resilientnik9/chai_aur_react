// whatever file you create in a vite app, it has to be '.jsx' as compared to '.js'
// also these files are called components
// also, again same as normal create react app project, here also, the export variable has no relation to the import variable name (dono alag bhi ho skta hai)

function myFunc(){
    return (<h1>
        Nikhil Anand, you have to do it!!
    </h1>);
}

export default myFunc;
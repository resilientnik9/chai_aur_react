// func name and export variable name should be same. although variable name here doesn't need to be same as in the file where we import this func.
function chai(){
    return (<h1>
        Let's learn React and finish it!!
    </h1>);
}

export default chai;